// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [
    createChatBotMessage(`Hi, Welcome to Mora UXplore 2.0`),
    createChatBotMessage(`First of all, I would like to know your name?`),
]
}

export default config